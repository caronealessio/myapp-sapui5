specVersion: "3.0"
metadata:
  name: fiori-ui5-app
type: application
server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      mountPath: /fiori
      afterMiddleware: compression
      configuration:
        baseUri: http://localhost:4004/fiori
